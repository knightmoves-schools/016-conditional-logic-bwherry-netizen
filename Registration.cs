namespace knightmoves;
public class Registration
{
    public int ValidateAge() {
    public bool oldEnough = true;
        
        if(oldEnough) = true {
            return "old enough";
        }
        
        if(oldEnough) = false { 
            return "";
        }   
    }
}

